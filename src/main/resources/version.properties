# Version information - populated by Maven filtering
project.version=${project.version}
project.name=${project.artifactId}
project.user.agent=${project.user.agent}
project.tracestrack.key=${tracestrack.api.key}
project.protomaps.key=${protomaps.api.key}

# Additional build information
build.timestamp=${maven.build.timestamp}